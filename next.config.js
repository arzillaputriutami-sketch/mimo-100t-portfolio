/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
  basePath: '/mimo-100t-portfolio',
  assetPrefix: '/mimo-100t-portfolio/',
}

module.exports = nextConfig
